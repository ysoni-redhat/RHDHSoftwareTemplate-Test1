---
apiVersion: apps/v1
kind: Deployment
namespace: ${{ values.namespace }}
metadata:
  name: ${{ values.projectName }}
  labels:
    app: ${{ values.projectName }}
  annotations:
    backstage.io/kubernetes-id: ${{ values.projectName }}
    backstage.io/kubernetes-namespace: ${{ values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${{ values.projectName }}
  template:
    metadata:
      labels:
        app: ${{ values.projectName }}
    spec:
      containers:
        - name: springboot
          image: image-registry.openshift-image-registry.svc:5000/demo-project/${{ values.projectName }}:latest
          ports:
            - containerPort: ${{ values.port }}
